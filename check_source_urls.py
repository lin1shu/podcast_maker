from pymongo import MongoClient; from bson.json_util import dumps; import json; client = MongoClient("mongodb://admin:password@localhost:27017/"); db = client["podcast_maker_db"]; docs = list(db["podcasts"].find({"source_url": {"$ne": ""}}).sort("created_at", -1).limit(5)); print(f"Found {len(docs)} documents with non-empty source_url"); for doc in docs: print(f"ID: {doc.get(\"_id\")}, URL: {doc.get(\"source_url\")}, Created: {doc.get(\"created_at\")}")
